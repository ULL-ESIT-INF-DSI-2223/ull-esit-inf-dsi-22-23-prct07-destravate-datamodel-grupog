import { expect } from "chai";
import { compareStrings, compareStringsIgnoreCase, compareStringsFirstIgnoringCase } from "../../src/utils/sort_func.js"

describe("Sorting functions", () => {
  // it("compareString with", () => {
  //   expect(compareStrings()).to.be.eql()
  // });

  // it("compareStringIgnoreCase with", () => {
  //   expect(compareStringsIgnoreCase()).to.be.eql()
  // });

  // it("compareStringsFirstIgnoringCase with", () => {
  //   expect(compareStringsFirstIgnoringCase()).to.be.eql()
  // });
});